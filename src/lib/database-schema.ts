/**
 * PostgreSQL Database Schema - Re-exported from postgres-schema.ts
 * This file maintains backward compatibility during migration
 * All types are now sourced from PostgreSQL (Neon) schema
 */

// Re-export everything from postgres-schema
export * from './postgres-schema';

// Log that we're using PostgreSQL
console.log('📦 Database schema redirected to PostgreSQL (Neon)');